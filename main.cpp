#include <iostream>

using namespace std;

void sort(int arr[], int size)
{
    
}

int main()
{
    int arr[] = {12,4,5,6,7,2,1};
    int size = sizeof(arr) / sizeof(arr[0]);
    sort(arr,size);
    return 0;
}